// Sections hidden for the first go-live (content not ready yet). Remove a key
// here to bring that section — and its nav link — back, no other code changes.
export const HIDDEN_SECTIONS = new Set(['merch', 'collaborate'])
